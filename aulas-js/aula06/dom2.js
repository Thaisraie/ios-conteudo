let elementoX = document.getElementById("elemento0");

let elemento = document.getElementsByClassName("elemento2");

for (let t = 0; t < elemento.length; t++) {
    elemento[t].style.color = 'red'
}

